{
  "name": "telegram-bot-notification-api",
  "type": "module",
  "version": "1.0.0",
  "description": "A simple API to send notification to Telegram chats using a bot.",
  "scripts": {
    "dev": "nodemon index.js",
    "setup-webhook": "node setup-webhook.js",
    "prestart": "node setup-webhook.js",
    "start": "node index.js",
    "lint": "eslint",
    "format": "eslint --fix"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "redis": "^5.10.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.16.1",
    "eslint": "^9.30.0",
    "nodemon": "^3.1.10"
  }
}